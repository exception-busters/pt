# 개선 보고서

---

## 지적사항 1. 통계에 주간통계와 월간통계가 없다

### 문제점
- 기록 탭에 캘린더 화면만 존재하고, 주간/월간 통계 기능이 없음
- 사용자가 자신의 운동 현황을 한눈에 파악하기 어려움

### 개선 내용

#### 1. 주간/월간 통계 화면 추가 (`statistics_screen.dart`)
- 주간 운동 통계: 총 운동 횟수, 총 시간, 일평균 운동, 소모 칼로리, 평균 강도, 일별 운동 차트
- 주간 식단 통계: 평균 칼로리, 목표 칼로리, 달성률, 영양소 평균(탄단지), 일별 식단 차트
- 월간 운동 통계: 총 운동 횟수, 주평균 운동, 완료율, 주별 운동 횟수 차트
- 월간 식단 통계: 평균 칼로리, 달성률, 하루 평균 식사, 주별 평균 칼로리 차트

#### 2. 통계 데이터 Provider 추가 (`statistics_providers.dart`)
- 주간/월간 운동 기록 조회 및 통계 계산 로직 구현

#### 3. 화면 구조 변경
| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 기록 탭 기본 화면 | 캘린더 | 주간/월간 통계 |
| 캘린더 접근 | - | 상단 캘린더 버튼 (📅) |

### 수정 파일
- `lib/features/records/presentation/statistics_screen.dart` - 주간/월간 통계 화면 신규 추가
- `lib/features/records/application/statistics_providers.dart` - 통계 데이터 Provider 추가
- `lib/features/records/domain/statistics_models.dart` - 통계 데이터 모델 추가
- `lib/core/router/app_router.dart` - 라우트 구조 변경
- `lib/features/records/presentation/records_screen.dart` - 캘린더 화면으로 역할 변경

### 개선 효과
- 기록 탭 진입 시 바로 주간/월간 통계 확인 가능
- 사용자의 운동 현황을 한눈에 파악할 수 있음

---

## 지적사항 2. 운동 자세 도우미 정확도가 떨어지는 점

### 문제점
- 운동 자세 평가에 사용되는 각도 기준값이 적절하지 않아 정확한 점수 산출이 어려움
- 올바른 자세를 취해도 낮은 점수가 나오거나, 잘못된 자세에서 높은 점수가 나오는 문제 발생

### 개선 내용

#### 1. 데이터 모델 확장 (`exercise_model.dart`)
- `MotionPhase` 클래스에 `phaseAngles` 필드 추가
- 각 페이즈별로 독립적인 각도 기준 설정 가능

#### 2. 페이즈 관리자 개선 (`phase_manager.dart`)
- `getCurrentPhaseAngles()` 메서드 추가
- 현재 페이즈에 맞는 각도 기준 반환

#### 3. 점수 계산 로직 수정 (`exercise_screen.dart`)
- 페이즈별 각도 기준으로 사용자 각도 계산
- `_calculateUserAnglesWithPhase()` 메서드 추가

#### 4. 운동 데이터 전면 수정 (`exercise_reference.json`)

| 운동 | 페이즈 | 주요 각도 기준 |
|------|--------|---------------|
| **스쿼트** | 시작 자세 | 무릎/힙 170° |
| | 하강 | 무릎/힙 95° |
| | 유지 | 무릎/힙 95° |
| | 상승 | 무릎/힙 135° |
| | 완료 | 무릎/힙 170° |
| **스탠딩 니업** | 시작 자세 | 양쪽 힙/무릎 170° |
| | 왼쪽 무릎 들기 | 좌측 90°, 우측 170° |
| | 왼쪽 내리기 | 양쪽 170° |
| | 오른쪽 무릎 들기 | 좌측 170°, 우측 90° |
| | 완료 | 양쪽 170° |
| **스탠딩 사이드 크런치** | 시작 자세 | 좌/우 상체 170° |
| | 좌측 굽히기 | 좌측 150°, 우측 170° |
| | 중앙 복귀 | 좌/우 170° |
| | 우측 굽히기 | 좌측 170°, 우측 150° |
| | 완료 | 좌/우 170° |

### 수정 파일
- `lib/models/exercise_model.dart`
- `lib/services/phase_manager.dart`
- `lib/screens/exercise_screen.dart`
- `assets/exercise_reference.json`

### 개선 효과
- 각 운동 동작 단계에 맞는 정확한 자세 평가 가능
- 올바른 자세 수행 시 적절한 점수 부여
- 자연스러운 페이즈 전환으로 운동 흐름 개선
