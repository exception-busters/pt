# 런치 스크린 에셋

이 디렉터리 안의 이미지를 원하는 파일로 교체하면 런치 스크린을 자유롭게 커스터마이징할 수 있습니다.

또는 `open ios/Runner.xcworkspace` 명령으로 Flutter 프로젝트의 Xcode 워크스페이스를 연 뒤, Project Navigator에서 `Runner/Assets.xcassets`를 선택하고 원하는 이미지를 드래그해 추가해도 됩니다.
