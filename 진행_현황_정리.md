# 진행 상황 정리 (Latest 2025-10-30)

- `tool/convert_food_db.py` 작성 및 실행 → `음식DB.xlsx` 기반 `assets/data/foods.json` 생성
- `pubspec.yaml`에 `assets/data/foods.json` 등록
- `lib/features/diet/application/diet_providers.dart` 확장  
  - FoodItem 모델, 사용자 프로필(DietUserProfile), 추천 엔진(DietRecommendationEngine) 및 `todayDietPlanProvider` 추가
- `lib/features/diet/presentation/diet_screen.dart` → 비동기 추천 UI로 교체 (추천 카드/로딩/에러 처리 포함)
- Supabase MCP 연동은 네트워크 제한(EAI_AGAIN)으로 보류. `~/.config/codex/codex.toml`에 Node 실행 경로를 직접 지정해두었으나, 외부 접속이 막혀 현재 실행 불가
