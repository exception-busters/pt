import 'package:flutter/material.dart';

// 배경 색상
const Color backgroundColor = Colors.white;

// 주요 텍스트 색상
const Color subTextColor = Color(0xFF666666); // 회색 계열 서브 텍스트

// 버튼 및 포인트 색상
const Color mainButtonColor = Colors.black;
const Color secondaryButtonColor = Color(0xFF333333); // 약간 밝은 블랙
const Color pointColor = Color(0xFF000000); // 포인트 컬러, 강조 필요할 때

// 경계선, 구분선 등
const Color borderColor = Color(0xFFE0E0E0); // 연한 그레이

